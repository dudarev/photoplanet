[uwsgi]

# master
master	= true
# maximum number of processes
processes = 2

virtualenv = {{ virtualenv_path }}

#path for manage.py
chdir = {{ application_path }}

# the socket (use the full path to be safe)
socket = /tmp/uwsgi.sock

# with appropriate permissions - *may* be needed
chmod-socket = 666
#path for settings from manage.py
<<<<<<< HEAD
env = DJANGO_SETTINGS_MODULE=photoplanet.settings.photoplanet
=======
env = DJANGO_SETTINGS_MODULE=photoplanet.settings
>>>>>>> a2006ac148ef64416feb57b6c787ff9acc558662
# Django's wsgi file
module = photoplanet.wsgi:application


# the virtualenv
#venv = /home/vagrant/www

daemonize = {{ application_log_dir }}/uwsgi.log 

uid = www-data

# group identifier of uWSGI processes
gid = www-data

# clear environment on exit
vacuum = true

#daemonize = {{ application_log_dir }}/uwsgi.log 
