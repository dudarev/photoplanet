[uwsgi]

# master
master	= true
# maximum number of processes
processes = 2

chdir = {{ application_path }}/{{ application_name }}
#echo $chdir
# the socket (use the full path to be safe)
socket = /tmp/uwsgi.sock

# with appropriate permissions - *may* be needed
#chmod-socket = 664

evn = DJANGO_SETTINGS_MODULE=photoplanet.settings.donetskogram
# Django's wsgi file
module = photoplanet.wsgi:application
#module = donetskogram.py 


# the virtualenv
#venv = /home/vagrant/www

daemonize = {{ application_log_dir }}/uwsgi.log 

uid = www-data

# group identifier of uWSGI processes
gid = www-data

# clear environment on exit
vacuum = true

#daemonize = {{ application_log_dir }}/uwsgi.log 
