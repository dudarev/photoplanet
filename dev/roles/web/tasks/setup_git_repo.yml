---

- name: Setup the Git repo
  git: repo={{ git_repo }} dest={{ project_path }} accept_hostkey=yes
  when: setup_git_repo
  tags: git
- name: cd
  command: cd {{ project_name }}
- name: Clone branch dev
  commang: git checkout -b dev origin/dev

